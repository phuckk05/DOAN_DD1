<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DA"
        tools:targetApi="31">
        -->
        <activity
            android:name=".activity.AuthActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
        ====================================================================
        MainActivity LÀ MÀN HÌNH BÊN TRONG ỨNG DỤNG
        Nó không phải là màn hình khởi động nữa nên ta XÓA intent-filter.
        Nó sẽ được gọi từ LoginFragment sau khi đăng nhập thành công.
        ====================================================================
        -->
        <activity
            android:name=".activity.MainActivity"
            android:exported="true">
            <!-- KHÔNG CÓ intent-filter ở đây -->
        </activity>
    </application>

</manifest>