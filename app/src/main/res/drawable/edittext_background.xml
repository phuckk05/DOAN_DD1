<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Màu nền cho EditText, bạn có thể chọn màu trắng hoặc một màu kem nhạt hơn -->
    <solid android:color="#FFFFFF" />

    <!-- Bo tròn các góc -->
    <corners android:radius="8dp" />

    <!-- Thêm một đường viền mảnh màu Accent -->
    <stroke
        android:width="1dp"
        android:color="@color/app_accent_mustard" />
</shape>
